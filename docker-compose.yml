services:
  frontend:
    build: ./frontend
    ports:
      - '3000:5173'
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - HOST=0.0.0.0
    command: pnpm dev --host --port 5173
